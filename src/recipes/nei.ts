/**
 * Requires: `import mods.nei.NEI;`
 */
export const hide = (id: string) =>
  `NEI.hide(${id});`;

/**
 * Requires: `import mods.nei.NEI;`
 */
export const addNEI = (id: string) =>
  `NEI.addEntry(${id});`;
